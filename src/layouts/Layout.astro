---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
  path?: string;
}

const {
  title = "Spartan Tyres",
  description = "Quality tyres, fast fitting, honest pricing.",
  path = Astro.url.pathname,
} = Astro.props;

const nav = [
  { href: "/", label: "Home" },
  { href: "/services/", label: "Services" },
  { href: "/about/", label: "About" },
  { href: "/contact/", label: "Contact" },
];

const normalized = path.endsWith("/") ? path : `${path}/`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/png" href="/img/logo-icon.png" />
    <link rel="apple-touch-icon" href="/img/logo-icon.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <title>{title}</title>
  </head>
  <body>
    <header class="site-header">
      <div class="inner">
        <a href="/" class="brand" aria-label="Spartan Tyres home"><img src="/img/logo.png" alt="Spartan Tyres" class="brand-logo" width="140" height="70" /></a>
        <nav class="nav">
          {nav.map((item) => (
            <a href={item.href} class={normalized === item.href ? "active" : ""}>{item.label}</a>
          ))}
        </nav>
      </div>
    </header>

    <main>
      <slot />
    </main>

    <footer class="site-footer">
      <div class="container">
        &copy; {new Date().getFullYear()} Spartan Tyres &middot; All rights reserved
      </div>
    </footer>
  </body>
</html>
